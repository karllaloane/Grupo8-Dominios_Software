package br.ufg.sep.state;

import br.ufg.sep.entity.Questao;
import jdk.jshell.execution.Util;

public class UtilQuestao {


    public static void copiarQuestao(Questao origem, Questao destino){
        /*
        destino.setState(null);
        destino.setAnexo(origem.getAnexo());
        destino.setConteudoEspecifico(origem.getConteudoEspecifico());
        destino.setEnunciado(origem.getEnunciado());
        destino.setProva(origem.getProva());
        destino.setNivelDificuldade(origem.getNivelDificuldade());
        destino.setState(null);
        destino.setJustificativa(origem.getJustificativa());
        destino.setIdQuestao(origem.getIdQuestao());
        */

    }

}
